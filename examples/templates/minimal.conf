# Minimal Template
# Git worktree only - no setup hooks run
# Useful for quick checkouts or when you want manual control

TEMPLATE_DESC="Minimal - git worktree only, no setup"

# Skip all setup hooks
WT_SKIP_DB=true
WT_SKIP_COMPOSER=true
WT_SKIP_NPM=true
WT_SKIP_BUILD=true
WT_SKIP_MIGRATE=true
WT_SKIP_HERD=true
