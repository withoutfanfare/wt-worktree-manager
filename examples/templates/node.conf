# Node.js Template
# For Node.js/JavaScript projects - npm only, no PHP or database

TEMPLATE_DESC="Node.js project (npm only, no PHP/database)"

# Skip PHP-related hooks
WT_SKIP_DB=true
WT_SKIP_COMPOSER=true
WT_SKIP_MIGRATE=true

# Keep npm and build enabled
WT_SKIP_NPM=false
WT_SKIP_BUILD=false
WT_SKIP_HERD=false
