# Laravel Template
# Full Laravel development environment with database, composer, npm, and migrations

TEMPLATE_DESC="Laravel with MySQL, Composer, NPM, and migrations"

# All hooks enabled (defaults)
WT_SKIP_DB=false
WT_SKIP_COMPOSER=false
WT_SKIP_NPM=false
WT_SKIP_BUILD=false
WT_SKIP_MIGRATE=false
WT_SKIP_HERD=false
