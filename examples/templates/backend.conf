# Backend Template
# For API-only or backend work - PHP and database, no frontend build

TEMPLATE_DESC="Backend only - PHP, database, no npm/build"

# Enable backend hooks
WT_SKIP_DB=false
WT_SKIP_COMPOSER=false
WT_SKIP_MIGRATE=false
WT_SKIP_HERD=false

# Skip frontend hooks
WT_SKIP_NPM=true
WT_SKIP_BUILD=true
